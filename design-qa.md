# 行情列表視覺驗證

範圍：使用者選定第一張設計的「最新有效報價」列表語言，套用搜尋結果、首頁漲跌排行、單品頁市場比價。頁首、品牌、首頁其他區塊與資料邏輯沿用原站。

final result: passed

此結果針對列表介面與已接入圖片的呈現。全品項圖片製作進度另以 `docs/ingredient-images/asset-manifest.json` 的 `coverage` 為準，未補齊品項沿用原本 CropIcon 圖示，不代表圖片資料集已全部完成。

## 視覺比對

已開啟 [選定設計](docs/ingredient-images/qa/selected-reference.png) 與 [同尺寸桌面截圖](docs/ingredient-images/qa/quote-search-target-viewport.png)，兩者皆 1487 × 1058。比較範圍是正常資料狀態下的報價列；原設計其餘版面不在使用者授權修改範圍。

通過項目：米白連續列、細分隔線、左側實物參考插畫、沿用站內字體的粗體品名、綠色大價格、價格與單位分層、方向箭頭搭配帶正負號的漲跌、右側日期與成交量資訊。原始行情品名、價格與日期替代設計稿的示意資料。

已修正的問題：

- 市場比價原本重複以作物名稱為主標題；改以市場名稱為主、作物名稱為副標題。
- 圖集留白曾露出相鄰格片段；依實際繪圖範圍量測，另加每格 CSS 裁切邊界，核對原 8 個邊界警示項目均無鄰格殘影。
- 首版插圖在不同祖先合成層出現白色方塊或雙重混色；改為圖片背景與列底色單次混色，桌面與手機確認正常。
- 首版動態色彩類名未被 Tailwind 收錄；改用完整字面類名，確認上漲橘色、下跌綠色。

## 響應式與可讀性

檢查 1440／1487 px 桌面及 390 × 844 手機。依使用者選擇，手機保留兩欄卡片；窄螢幕每張卡片內將品名、價格與漲跌垂直排列，將漲跌放在價格下方，日期與成交量留在列底部，保留完整品名與可點擊範圍。檢查 [首頁手機](docs/ingredient-images/qa/quote-home-mobile.png)、[市場比價手機](docs/ingredient-images/qa/quote-markets-mobile-blend.png) 和 [深色搜尋](docs/ingredient-images/qa/quote-search-dark.png)。

深色模式使用淺綠價格、淺橘上漲與可讀的次要資訊；圖片保留明亮小底板，不反轉食材顏色。相鄰可見品名提供圖片語意，圖片本身不重複朗讀。漲跌除顏色外亦有箭頭、正負號與輔助文字；缺少比較資料時顯示「尚無比較資料」。鍵盤焦點樣式可見，內容不依賴新增入場動畫。

## 行為與檢查

`npm run typecheck`、`npm run lint` 通過。現有 `search-price-change`、`produce-data-state` Playwright 測試於 Chromium 與 mobile Safari 共 10 項通過，涵蓋報價漲跌與資料狀態。驗證使用路由攔截資料，沒有修改生產資料或 fixtures。

搜尋頁瀏覽器 error 清單為空。原有列連結、搜尋條件、排序和分頁保留。圖片驗證器檢查來源 URL、圖片檔案、分類隔離、名稱／代碼查表、有限裁切範圍，以及休市不得配圖。

## 後續

依使用者要求停止找圖，本次已完成的圖集整理接入；未完成品項保留原本圖示。不同作物外觀與未確認交易名稱保持獨立，不能為提高覆蓋率而配上無法佐證的圖片。新增圖集後須重跑圖片驗證並目視檢查裁切。

最終收尾：657 插圖／758 個名稱對應；圖片驗證、型別、lint、Chromium 與 mobile Safari 共 10 項測試通過。測試初次因本機開發伺服器停止而無法連線，重啟 3107 後全部通過。
